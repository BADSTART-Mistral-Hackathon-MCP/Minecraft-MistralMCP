# Server configuration
PORT=3001

# Minecraft server configuration
MC_HOST=localhost
MC_PORT=25565
MC_VERSION=1.21.1

# Auth mode: 'offline' (local server) or 'microsoft' (online)
MC_AUTH=offline

# For offline mode, this is the bot name used in-game
MC_USERNAME=BridgeBot

# For Microsoft auth, you can optionally provide your Microsoft email
# (Mineflayer uses a device code flow; password is NOT used.)
# If not set, a placeholder is used and will be replaced after login.
# MC_EMAIL=you@example.com

# Optional: where to cache Microsoft tokens (created automatically)
# MC_PROFILES_DIR=.minecraft-profiles
